<section class="forms">
    <% if (title) { %>
        <h1><%= title %></h1>
        <% } else {
          res.redirect('/')
        } %>
    
    <%- messages() %>
    <% if (errors) { %>
      <ul class="notice">
     <% errors.array().forEach(error => { %>
       <li><%= error.msg %></li>
    <%  }) %>
     </ul>
    <% } %>
    <h2> Update Account</h2>
      <form class="general-form" id="updateForm" action="/account/update-info" method="post">
        <label for="account_firstname" >First Name :</label>
        <input id="account_firstname" type="text" name="account_firstname" placeholder="Stuart" required value="<%= locals.account_firstname %>">
        <label for="account_lastname">Last Name :</label><input id="account_lastname" name="account_lastname" type="text" placeholder="Little" required value="<%= locals.account_lastname %>">
        <label for="account_email">Email Address :</label><input id="account_email" name="account_email" type="email" placeholder="john_johnson@email.com" required value="<%= locals.account_email %>">
        <input type="hidden" name="account_id" <% if(locals.account_id) { %> value="<%= locals.account_id %>" <% } %> >
        <button type="submit" class="login-btn">Update</button>
    </form>    
</section>

<section class="forms">

    <h2>Change Password</h2>
      <form class="general-form" id="passwordForm" action="/account/update-password" method="post">
        <label for="account_password">Password :</label>
        <span class="form-notes"><strong>Note:</strong> Passwords must be at least 12 characters and contain at least 1 number, 1 capital letter and 1 special character</span> 
        <input id="account_password" name="account_password" type="password" required pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$">
        <input type="hidden" name="account_id" <% if(locals.account_id) { %> value="<%= locals.account_id %>" <% } %> >
        <button type="submit" class="login-btn">Change Password</button>
    </form>

    <!-- <h2>Delete Account</h2>
    <form class="login" id="passwordForm" action="/account/update-password" method="post">
      <h3><strong>NOTE: DELETING AN ACCOUNT IS IRREVERSIBLE</strong></h3>
      <button type="submit" class="login-btn">Delete Account</button>
  </form> -->
    
</section>